# Visual Gate

Use the `/golden` route as the first reference surface.

Capture the page at 1440px and 390px widths. Compare composition, typography, media geometry, spacing, controls, borders, palette and responsive behavior with the approved reference images.

When the Owner accepts the result, record the reference images, screenshots and commit SHA. That accepted commit becomes the visual implementation source for later storefront integration.
