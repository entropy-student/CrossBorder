# The canonical source project is versioned independently.
CrossBorder-Independent-Store/

# Reviewer packages and disposable staging are generated outputs.
*.zip
*_STAGING*/
_review_stage/
_review_outbox/
FULL-REVIEW-CHECKPOINT-002*/

# Never track runtime, environment or credential material at the document center.
.runtime/
.env
.env.*
credentials/
credentials*/
*credential*
*.local.txt
