# Payment Provider Decision

Current status: `UNDECIDED`.

The project will first inspect another already-running system that the Owner intends to reuse for payment and fulfillment. No final provider or adapter type is selected yet.

Existing PayPal and WorldFirst material remains reference/history only. Existing code stays disabled unless a later Reviewer Gate explicitly reactivates it.

When work resumes, the project must inspect the external system's real capabilities, ownership boundaries, callbacks, retry behavior and fulfillment responsibilities before selecting an integration shape.
